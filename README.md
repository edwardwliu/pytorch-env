# Minimal Python Virtual Env for PyTorch

Run the following script to:
1. Create a python virtual environment `pytorch-env`
2. Install the necessary PyTorch packages.
3. Activate the venv  `pytorch-env`.

```
source setup_env.sh
```
